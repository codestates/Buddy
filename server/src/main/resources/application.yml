sever:
  port: 8080
spring:
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:mysql://13.209.97.150:3306/buddy?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: admin
    password: buddy1357
    driver-class-name: com.mysql.cj.jdbc.Driver

  config:
    domain: https://yana-buddy.com/

jwt:
  secret: buddykey
